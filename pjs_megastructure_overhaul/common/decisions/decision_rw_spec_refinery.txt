decision_rw_spec_refinery = {
	owned_planets_only = yes

	prerequisites = {
		"tech_rw_spec_refinery"
	}

	enactment_time = 360
	resources = {
		category = decisions
		cost = {
			influence = 100
			alloys = 1000
		}
	}

	potential = {
        is_planet_class = pc_ringworld_habitable
		owner = { has_ascension_perk = ap_master_builders }
        AND = {
            NOT = { has_planet_flag = rw_spec_empire }
            NOT = { has_planet_flag = rw_spec_unity }
            NOT = { has_planet_flag = rw_spec_refinery }
            NOT = { has_planet_flag = rw_spec_science }
        }
	}

    effect = {
        set_planet_flag = rw_spec_refinery
	}

	ai_weight = {
		weight = 25
	}
}
