namespace = pjs_dune_gamestart_event

event = {
	id = pjs_dune_gamestart_event.1

	hide_window = yes
	is_triggered_only = yes

	immediate = {
		if = {
			limit = { NOT = { any_system = { has_star_flag = pjs_dune_delta_pavonis } } }
			random_system = { spawn_system = { initializer = misc_pjs_delta_pavonis } }
		}
		if = {
			limit = { NOT = { any_system = { has_star_flag = pjs_dune_ophiuchi } } }
			random_system = { spawn_system = { initializer = misc_pjs_ophiuchi } }
		}
		if = {
			limit = { NOT = { any_system = { has_star_flag = pjs_dune_tau_haruki } } }
			random_system = { spawn_system = { initializer = misc_pjs_tau_haruki } }
		}
	}
}
