decision_rw_spec_culture = {
	owned_planets_only = yes

	prerequisites = {
		"tech_rw_spec_culture"
	}

	enactment_time = 360
	resources = {
		category = decisions
		cost = {
			influence = 25
			alloys = 1000
		}
	}

	potential = {
        is_planet_class = pc_ringworld_habitable
		from = {
		    is_regular_empire = yes
		}
		owner = {
            has_ascension_perk = ap_master_builders
		}
        AND = {
            NOT = { has_planet_flag = rw_spec_arms_industry }
            NOT = { has_planet_flag = rw_spec_bureaucracy }
            NOT = { has_planet_flag = rw_spec_civilian_industry }
            NOT = { has_planet_flag = rw_spec_culture }
            NOT = { has_planet_flag = rw_spec_science }
        }
	}

    effect = {
        set_planet_flag = rw_spec_culture
	}

	ai_weight = {
		weight = 50
	}
}
