namespace = pjs_traits_asari_event

event = {
	id = pjs_traits_asari_event.1

	hide_window = yes
	is_triggered_only = yes

	immediate = {
		every_country = {
			limit = { is_asari_trait = yes }
			country_event = { id = pjs_traits_asari_event.2 }
		}
	}
}

country_event = {
	id = pjs_traits_asari_event.2

	hide_window = yes
	is_triggered_only = yes

	immediate = {
        give_technology = { tech = "tech_psionic_theory" message = no }
        give_technology = { tech = "tech_alien_life_studies" message = no }
	}
}
