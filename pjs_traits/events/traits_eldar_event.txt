namespace = traits_eldar_event

event = {
	id = traits_eldar_event.1

	hide_window = yes
	is_triggered_only = yes

	immediate = {
		every_country = {
			limit = { is_eldar_trait = yes }
			country_event = { id = traits_eldar_event.2 }
		}
	}
}

country_event = {
	id = traits_eldar_event.2

	hide_window = yes
	is_triggered_only = yes

	immediate = {
        give_technology = { tech = "tech_psionic_theory" message = no }
        give_technology = { tech = "tech_experimental_subspace_navigation" message = no }
        give_technology = { tech = "tech_wormhole_stabilization" message = no }
        give_technology = { tech = "tech_alien_life_studies" message = no }
	}
}
