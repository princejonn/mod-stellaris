namespace = traits_inert_psionic_event

event = {
	id = traits_inert_psionic_event.1

	hide_window = yes
	is_triggered_only = yes

	immediate = {
		every_country = {
			limit = { is_inert_psionic_trait = yes }
			country_event = { id = traits_inert_psionic_event.2 }
		}
	}
}

country_event = {
	id = traits_inert_psionic_event.2

	hide_window = yes
	is_triggered_only = yes

	immediate = {
        add_research_option = tech_psionic_theory
	}
}
